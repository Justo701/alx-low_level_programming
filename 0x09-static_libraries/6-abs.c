#include "main.h"
/**
 * _abs - computes an absolute value of an integer
 * @n: the number in question
 * Return: abs value of int.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

