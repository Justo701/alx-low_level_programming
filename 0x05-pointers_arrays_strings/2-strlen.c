
#include"main.h"

/**
* _strlen - return the lenth of a string
* @s: input string
* Return: length of a string
*/

int_strlen(char *s)
{
int index;


for (index = 0; s[index] != '\0'; index++)
return (index);
}
