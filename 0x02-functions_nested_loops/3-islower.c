#include "main.h"
/**
 * _islower - checks if a char is lowecase
 * @c: is the character to be checked
 * Return: 1 if char is lowercase, otherwise return 0
 */
int _is lowercase(int c)
{
	if (c > 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
